type ButtonSize = 'small' | 'medium' | 'large'

type ButtonType = 'button' | 'reset' | 'submit'

type ButtonVariant =
  | 'filled'
  | 'outlined'
  | 'ghost'

type ButtonColor = 'blue' | 'green' | 'orange' | 'red' | 'grey'

export type { ButtonSize, ButtonType, ButtonVariant, ButtonColor }
